/**
 * Sum two numbers
 * @param {number} n - First number
 * @param {number} m - Second number
 * @returns {number} The sum of n and m
 */
function magicSum(n, m) {
  return n + m;
}

module.exports = { magicSum };

